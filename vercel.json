{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npm run build:prod:client -- --env=prod",
  "outputDirectory": "dist/browser",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
